import { FilterSucursalesPipe } from './filter-sucursales.pipe';

describe('FilterSucursalesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSucursalesPipe();
    expect(pipe).toBeTruthy();
  });
});
